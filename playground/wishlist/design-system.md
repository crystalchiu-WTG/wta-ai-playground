# Design-system wishlist

Gaps designers have hit when prompting Claude — colours, tokens, components, or patterns that weren't available. The design-system team uses this as input when next syncing tokens or extending the component library.

Claude appends entries automatically when a requested value isn't in the canonical token / component source. The closest existing token or component is used in the meantime, so the prototype still renders something.

| Requested | Where (page / component) | Closest used | Date | Original prompt |
|---|---|---|---|---|
| _(none yet)_ | | | | |

---

**For the design-system team:** when you next sync tokens / components from Figma (or wherever your source of truth lives), run through this list and either (a) add the missing token / component, or (b) document why the closest match is correct. Then clear the entries you've resolved.
